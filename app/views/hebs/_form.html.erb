<%= simple_form_for(@heb) do |h| %>

  <%= h.input :name, label: "Nom", placeholder: "Nom de l'hébergement" %>

  <%= h.input :address, label: "Adresse", placeholder: '13 rue Morand, Paris'  %>

  <%= h.input :description, label: 'Description' %>
  <%= h.submit "Valider", class: "btn btn-foyer"  %>
<% end %>

